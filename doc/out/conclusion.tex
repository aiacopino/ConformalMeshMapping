\section{Korrigenda}
- wenn pf nur referenziert: referenz in der überschrift vom thm statt unten, pf unten ganz weglassen
- definitionen auch referenzieren
- auch bei synthese aus mehreren quellen alle quellen angeben
- referenz von riemann mapping thm proof
- zitate detaillierter, thm nr statt page nr
- hölderspaces: nicht theorie sondern zitat zur definition von wegmann
- definition von mesh: aus irgend einem FEM buch (gemeinsame kante von anliegenden elementen, jede kante und jeder knoten gehört zu mind einer zelle)
- sobolev spaces erste equation tippfehler
- alternating projections: welche domain vom sobolev raum zB L²[0,2pi]? und raum normalerweise H1 statt W als sobolevraum. und was genau macht AP? in prosa (convex sets in hilbert space zu allg, müssten konkrete mengen im fkt raum sein). notieren dass sobolev 2pi-periodische fkt beinhaltet falls dem so ist
- zuerst überlegen, dann programm schreiben
- geeignete datenstrukturen?
- modularer code!
- gleichzeitig tests schreiben wie code, für die modularen fkt und auch für gesamtalgo um korrektheit zu prüfen
- if there is a known mapping e.g. from square to disk given as an explicit formula, use this for testing of correctness
- speziell konvergenzaussagen related to metrik ("abstand zum limes") spezifizieren und genau zitieren
- auf papier, implementieren, testen, dann melden sonst wenns probleme gibt
- boundary is real array of fourier coefficients, trig poly = truncated fourier series
- boundary is almost smooth since we cannot imput infinite fourier coefficients
- mesh format: Nx2 matrix of points (coordinates of N nodes) plus Mx3 matrix of integers (indices of nodes constituing each of M triangles)

future work: maybe mention more niche methods which were not covered for time reasons
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\iffalse
done:
x note in the beginning thaty R²=C

a deformation psi von x,y klar angeben= u + iv such that Jpsi = 0.
As a byproduct of the Cauchy-Riemann equ
psi(z)=psi(x,y) klar machen z komplex, x,y reell   

eq 3 u = Re(eta) nicht re(psi)=eta

This problem has a unique solution up to an imaginary constant falsch: realteil eindeutig, imaginärteil wird dann hergeleitet und ist bis auf const auch eondeutig

definition 8 falsch g sollte phi sein 

check finite element mesh arxiv

differentiability of zipper method construction? sollte gehen
jacobi of zipper using back propagation?

check all methods are present in overview flowchart
add section numbers

put in each method's description what exactly it gets its restriction from, e.g. pinpoint the bottleneck







\fi